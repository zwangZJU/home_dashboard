version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: devtrack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DEVTRACK_DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/devtrack
    depends_on:
      - db

volumes:
  pgdata:
